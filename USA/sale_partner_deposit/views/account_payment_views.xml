<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="account_payment_sale_deposit_view_form" model="ir.ui.view">
        <field name="name">account.payment.sale.deposit.view.form</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account_partner_deposit.view_account_payment_deposit_form"/>
        <field name="arch" type="xml">
            <field name="ref" position="after">
                <field name="sale_deposit_id"
                       attrs="{'readonly': ['|', ('state', '!=', 'draft'), ('partner_id', '=', False)], 'invisible': ['|', ('is_deposit', '!=', True), ('partner_type', '!=', 'customer')]}"
                       options="{'no_create': True}" force_save="1"/>
            </field>
        </field>
    </record>
</odoo>
